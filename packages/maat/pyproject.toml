[project]
name = "maat"
version = "0.1.0"
description = ""
authors = [{name = "Will Fitzgerald"}]
requires-python = ">=3.11"

dependencies = [
    "lxml>=5.2.2",
    "lingua-language-detector>=2.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["maat"]