[project]
name = "hcb_infilling"
version = "0.1.0"
description = ""
authors = [{name = "Gabriele Giannessi"}]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hcb_infilling"]