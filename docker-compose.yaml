services:
  gs-api: 
    build:
      context: .
      dockerfile: Dockerfile
    image: gabrielegiannessi/gs-api:latest
    command: sh -c 'uvicorn api.main:app --reload --host 0.0.0.0 --port 8000'
    ports:
      - 8000:8000
    volumes:
      - .:/app  